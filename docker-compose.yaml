version: "3.9"

services:
  app:
    build: .
    container_name: flask_app
    ports:
      - "8000:8000"
    environment:
      - FLASK_ENV=development
    command: ["gunicorn", "app:app", "--bind", "0.0.0.0:8000", "--workers", "4"]
